1. create virtual environment
2. create project

- standard or uvicorn

3. route

4. Teardown: dọn dẹp tài nguyên sau khi xử lý xong request

5. Yield:
   Setup tài nguyên trước request
   Teardown (dọn dẹp) sau request
   code sau Yield chạy su endpoint
6. async await
7. Schema (DTO)
8. Db Model
